{% extends 'base.html.twig' %}

{% block title %}Borrow{% endblock %}

{% block body %}
    <h1>Borrow</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ borrow.id }}</td>
            </tr>
            <tr>
                <th>Startdate</th>
                <td>{{ borrow.startdate ? borrow.startdate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Enddate</th>
                <td>{{ borrow.enddate ? borrow.enddate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_borrow_index') }}">back to list</a>

    <a href="{{ path('app_borrow_edit', {'id': borrow.id}) }}">edit</a>

    {{ include('borrow/_delete_form.html.twig') }}
{% endblock %}
