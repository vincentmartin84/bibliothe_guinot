<form method="post" action="{{ path('app_available_delete', {'id': available.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ available.id) }}">
    <button class="btn">Delete</button>
</form>
